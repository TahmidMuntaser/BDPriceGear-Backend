name: Populate Database (DISABLED)

on:
  # DISABLED: Playwright browser installation keeps failing in GitHub Actions
  # To manually populate:
  # 1. Run locally: python manage.py populate_products --limit 50
  # 2. Then run: python migrate_to_supabase.py
  # For now, database has 1,417 products and is stable
  
  workflow_dispatch:

jobs:
  populate:
    runs-on: ubuntu-latest
    steps:
      - name: ℹ️ Workflow Disabled
        run: |
          echo "⚠️ Database population workflow is disabled"
          echo ""
          echo "Reason: Playwright browser installation fails in GitHub Actions"
          echo ""
          echo "To update products manually:"
          echo "1. Run locally: python manage.py populate_products --limit 50"
          echo "2. Export: python manage.py dumpdata products > backup.json"
          echo "3. Load to Supabase: python migrate_to_supabase.py"
          echo ""
          echo "Current Database Status:"
          echo "✅ 1,417 products loaded"
          echo "✅ 11 categories loaded"
          echo "✅ 7 shops loaded"
          echo "✅ Data persisted in Supabase"

name: Hourly Product Scraping (DISABLED)

on:
  # DISABLED: Playwright browser installation keeps failing in CI/CD
  # To re-enable, uncomment the schedule and workflow_dispatch below
  # schedule:
  #   # Run every hour at minute 0 (0 0 * * *)
  #   - cron: '0 * * * *'
  # workflow_dispatch:
  
  # Placeholder to keep workflow syntax valid
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - name: ℹ️ Workflow Disabled
        run: |
          echo "⚠️ Playwright scraping workflow is disabled"
          echo ""
          echo "Reason: Browser installation fails in GitHub Actions CI/CD"
          echo ""
          echo "Alternative solutions:"
          echo "1. Run 'python manage.py populate_products' locally"
          echo "2. Use GitHub Codespaces with proper browser support"
          echo "3. Deploy scraper as separate service with pre-built Docker image"
          echo ""
          echo "Current Status:"
          echo "✅ 1,417 products are loaded in Supabase"
          echo "✅ Render API is live and serving data"
          echo "✅ Data persists across deployments"
